- running length for enc

- next run ece

# 1. Non-pca reduced performance is slightly stronger or similarly strong. More complete no-pca results would make the conclusion stronger

- DONE: table comparing pca vs non-pca for llp and clp across datasets

- DO: reduce the table to md and then add to r1.md

---

2. How sensitive is the method to text length? 

- DO: run an experiment on text length on DetectRL

- DO: run bert baseline


---

3. The paper only used four indicators to characterize the quality of the texts generated by humans and machines, which might have overlooked other features. 

- DO: add a plot of curvature as a fifth metric

---

4. How the detection performance changes with the parameter scale of the large language model or the specific architectural design choices.

- DO:run 2 other sizes for the ablation on TSM

5. W3: When evaluating the strength of AI editing, the paper did not conduct a direct benchmark comparison between it and specialized multi-classification or regression detection models.

- DO: see how editlens did this; then use theirs and a multiclass model baseline as from the paper and run against the probe; one dataset should be enough

6. I would also suggest adding calibration/error analysis for false positives on human text, especially multilingual and edited human text, since high AUC alone may hide harmful deployment behavior.

- DO: need to ask gpt